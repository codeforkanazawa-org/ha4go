<div class="column__top">
    <div class="jumbotron">
        <div class="container">
            <div class="caption col-md-8 col-sm-12">
                <img src="http://www.codeforkanazawa.org/images/logo.png" alt="Code for Kanazawa">
                <div class="caption__text">
                    <p>Facebookでの課題管理はしんどいので<br>新しいツール、つくりました。</p>
                    <% unless authorized? %>
                        <p><%= link_to('Facebookで登録', '/auth/facebook', id: 'sign_in', :class => 'btn btn-primary btn-lg') %></p>
                    <% else %>
                        <p><%= link_to('使い方を見る', help_path, :class => 'btn btn-primary btn-lg') %></p>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="container">

    <% if notice %>
        <div class="alert alert-success" role="alert">
            <%= notice %>
        </div>
    <% end %>

    <h2>最近の課題</h2>
    <% if authorized? %>
        <%= link_to '課題を投稿する', new_project_path , :class => "btn btn-default btn-primary"%>
    <% end %>
    <div class="panel-group">
        <% @projects.each do |project| %>
            <h3><%= link_to(project.subject, project_path(id: project.id)) %></h3>
            <p><span class="small"><%= name_and_link(project.user) %>さんが <%= project.created_at.strftime("%Y-%m-%d %H:%M") %>に投稿</span></p>
        <div class="panel panel-default">
            <div class="panel-body">
                <%= project.description %>
            </div>
        </div>
        <% end %>
        <%= link_to '課題一覧をみる', projects_path, :class => "btn btn-default" %>
    </div>
</div>
